@if(Request.IsAuthenticated) {
    <text>Вы вошли как <strong>@User.Identity.Name</strong>
    @(User.IsInRole("Editor") ? "(Editor)" : String.Empty)
    [ @Html.ActionLink("Панель управления", "Index", "Article") ]
    [ @Html.ActionLink("Выход", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Вход", "LogOn", "Account") ]
}
